import styled from '@emotion/styled';

const Container = styled.div`
  min-height: 52rem;
  display: flex;
  align-items: center;
  padding: 0 16rem;
`;

const ProfileImage = styled.img`
  width: 36rem;
`;

const Introduce = styled.div``;

export { Container, ProfileImage, Introduce };
